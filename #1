#1 Напишите программу, которая принимает на вход цифру, обозначающую день недели, и проверяет, является ли этот день выходным.

print('Input Number of the day: ')
a = int(input())
if (a >= 6 and a <= 7):
    print('Введенный день является выходным днем')
if (a >= 1 and a <= 5):
    print ('Введенный день является рабочим днем')
if (a < 1 or a > 7):
    print('Такого дня недели не бывает')



#2 Напишите программу для. проверки истинности утверждения ¬(X ⋁ Y ⋁ Z) = ¬X ⋀ ¬Y ⋀ ¬Z для всех значений предикат.

x = True
y = True
z = True

for x in [0, 1]:
    for y in [0, 1]:
        for z in [0, 1]:
            print("¬(",x, "V", y, "V", z,") = ¬",x,"¬ ⋀", y, "¬ ⋀", z)
            print(not(x and y and z) == (not x) or (not y) or (not z))


#3 Напишите программу, которая принимает на вход координаты точки (X и Y), причём X ≠ 0 и Y ≠ 0 и выдаёт номер четверти плоскости, в которой находится эта точка.

print("Введите значение X: ")
x = int (input())
print("Введите значение Y: ")
y = int (input())

if(x > 0 and y > 0):
    print("Точка с заданными координатами находится в I четверти")
if(x < 0 and y > 0):
    print("Точка с заданными координатами находится во II четверти")
if(x < 0 and y < 0):
    print("Точка с заданными координатами находится в III четверти")
if(x > 0 and y < 0):
    print("Точка с заданными координатами находится в IV четверти")
    

#4 Напишите программу, которая по заданному номеру четверти, показывает диапазон возможных координат точек в этой четверти (x и y).

print("Введите номер четверти: ")
number = int (input())

if(number == 1):
    print("Диапазон возможных координат I четверти: (x > 0), (y > 0)")
if(number == 2):
    print("Диапазон возможных координат II четверти: (x < 0), (y > 0)")
if(number == 3):
    print("Диапазон возможных координат III четверти: (x < 0), (y < 0)")
if(number == 4):
    print("Диапазон возможных координат IV четверти: (x > 0), (y < 0)")


#5 Напишите программу, которая принимает на вход координаты двух точек и находит расстояние между ними в 2D пространстве.
# Пример:
# - A (3,6); B (2,1) -> 5,09
# - A (7,-5); B (1,-1) -> 7,21

print("Введите координаты первой точки через пробел: ")
a = input().split()
print("Введите координаты второй точки через пробел: ")
b = input().split()

a_int = list(map(int, a))
b_int = list(map(int, b))

print("Координаты точки а: ", a_int)
print("Координаты точки b: ", b_int)
ab = ((b_int[0] - a_int[0])**2 + (b_int[1] - a_int[1])**2)**(0.5)
print("Расстояние между точками a и b равно ", round(ab, 2))
